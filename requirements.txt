# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19

# Image processing
Pillow>=10.0.0
# rembg[cpu]>=2.0.0  # disabled: needs >512MB RAM, exceeds Railway free tier

# HTTP client (for Replicate API)
httpx>=0.27.0

# HuggingFace Spaces client (FREE inference)
gradio_client>=1.0.0

# Utils
pydantic>=2.0.0
